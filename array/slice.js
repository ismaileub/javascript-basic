/*
    slice means take a particular part from an array
    syntax:slice(starting index, end at which index before)
*/


const numbers = [1, 2, 10 , 15, 20, 25, 30];

console.log(numbers.slice(1,4));

console.log(numbers.slice(2));